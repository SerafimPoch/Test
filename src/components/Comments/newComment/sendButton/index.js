import React from "react";
import { SendButtonContainer, SendButton } from "./style";

export default () => {
  return (
    <SendButtonContainer>
      <SendButton>Send</SendButton>
    </SendButtonContainer>
  );
};
